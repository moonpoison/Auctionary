<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.sm.repository.PointHistoryRepository">

    <insert id="insert" parameterType="edu.sm.dto.Point_History">
        INSERT INTO point_history (
            user_id, action_type, point_change, final_point, change_date, note
        ) VALUES (
             #{userId}, #{actionType}, #{pointChange}, #{finalPoint}, #{changeDate}, #{note}
                 )
    </insert>

    <select id="select" parameterType="String" resultType="edu.sm.dto.Point_History">
        SELECT *
        FROM point_history
        WHERE user_id = #{userId}
        ORDER BY change_date DESC
            LIMIT 1
    </select>

    <select id="selectAll" parameterType="String" resultType="edu.sm.dto.Point_History">
        SELECT
            user_id as userId,
            action_type as actionType,
            point_change as pointChange,
            final_point as finalPoint,
            change_date as changeDate,
            note
        FROM point_history
        WHERE user_id = #{userId}
        ORDER BY change_date DESC
    </select>

</mapper>