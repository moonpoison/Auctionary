<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.sm.repository.ProductRepository">

    <select id="findBiddingProductsByUserId" parameterType="String" resultType="Product">
        SELECT DISTINCT
            p.*
        FROM Product p
                 JOIN Bid b ON p.product_id = b.product_id
        WHERE b.user_id = #{userId}
        ORDER BY p.auction_end_date DESC
    </select>

    <select id="findSellingProductsByUserId" parameterType="String" resultType="Product">
        SELECT *
        FROM Product
        WHERE user_id = #{userId}
        ORDER BY auction_start_date DESC
    </select>
</mapper>