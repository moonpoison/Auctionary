<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="edu.sm.repository.UserRepository">

    <!-- 단일 조회 (기본키: user_id) -->
    <select id="select" parameterType="String" resultType="User">
        SELECT * FROM User WHERE user_id = #{userId}
    </select>

    <!-- 전체 조회 -->
    <select id="selectAll" resultType="User">
        SELECT * FROM User
    </select>

    <!-- 사용자 등록 -->
    <insert id="insert" parameterType="User">
        INSERT INTO User (
            user_id, password, name, birth_date, phone_number, email, role
        ) VALUES (
                     #{userId}, #{password}, #{name}, #{birthDate}, #{phoneNumber}, #{email}, #{role}
                 )
    </insert>

    <!-- 사용자 정보 수정 -->
    <update id="update" parameterType="User">
        UPDATE User
        SET password = #{password},
            name = #{name},
            birth_date = #{birthDate},
            phone_number = #{phoneNumber},
            email = #{email},
            role = #{role}
        WHERE user_id = #{userId}
    </update>

    <!-- 사용자 삭제 -->
    <delete id="delete" parameterType="String">
        DELETE FROM User WHERE user_id = #{userId}
    </delete>

    <!-- 이메일 기준 사용자 조회 (로그인 등) -->
    <select id="selectByEmail" parameterType="String" resultType="User">
        SELECT * FROM User WHERE email = #{email}
    </select>

</mapper>
